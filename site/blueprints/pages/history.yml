title: History
template: article

# and this is a comment
# use kirby vids to follow along and create a well commententated .yml
# file that you can just copy / paste sections.

columns:
  main:
    width: 2/3
    sections:
      fields:
        type: fields
        fields:
          text:
            type: textarea
            size: huge
  sidebar:
    width: 1/3
    sections:
      pages:
        type: pages
        template: History
        status: published
        layout: list
      files:
        type: files
